import { Container } from '@/components/container';
import { MEPManagementContent } from './MEPManagementContent';

const MEPManagementPage = () => {
  return (
    <Container>
      <MEPManagementContent />
    </Container>
  );
};

export { MEPManagementPage };
